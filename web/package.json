{
  "name": "go-starter-web",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "generate:api": "openapi-typescript ../api/openapi.yaml -o app/shared/api/generated/schema.ts",
    "typecheck": "nuxt typecheck",
    "test": "vitest",
    "test:run": "vitest run"
  },
  "dependencies": {
    "nuxt": "^4.5.2",
    "vue": "^3.5.0",
    "vue-router": "^4.5.0"
  },
  "devDependencies": {
    "openapi-typescript": "^7.13.0",
    "typescript": "^5.9.0",
    "vitest": "^4.1.0",
    "vue-tsc": "^3.1.0"
  }
}
